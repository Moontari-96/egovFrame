<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="egovframework.example.korea.menu.service.impl.menuMapper">
  <select id="selectActiveAll" resultType="MenuVO">
    select 
    	menu_id, 
    	parent_id, 
    	menu_depth, 
    	menu_name, 
    	menu_url,
        sort_order, 
        is_active
    from
    	menu
    where
    	is_active = 1
   	order by 
   		menu_depth asc,
        coalesce(parent_id,0) asc,
        sort_order asc,
        menu_id asc
  </select>
</mapper>